/** `npm run check:growth` — 리뷰 요청 조건과 공유 문구가 깨지면 여기서 터져요. */
import { demo as demoReview } from "../src/lib/review.ts";
import { demo as demoShare } from "../src/lib/share.ts";

demoReview();
demoShare();
